<div id="uploadModal" class="modal">
  <div class="modal-content">
    <span class="close-button" id="closeUploadModal">&times;</span>
    <form action="/upload" method="POST" enctype="multipart/form-data">
      <h2>Încarcă imagine de profil</h2>
      <input type="file" name="profilePic" accept="image/*" required />
      <button type="submit">Upload</button>
    </form>
  </div>
</div>
<style>
  .modalUpload {
    display: none; /* Hidden by default */
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw; /* Full width */
    height: 100vh; /* Full height */
    background-color: rgba(0, 0, 0, 0.6); /* Overlay effect */
    z-index: 9999;
  }
  #uploadModal.modal {
    display: none;
    position: fixed;
    z-index: 1001;
    background-color: rgba(0, 0, 0, 0.6); /* Fullscreen dark background */
  }
  #uploadModal .modal-content {
    background-color: #fff;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
    width: 100%;
    max-width: 400px;
    position: relative;
    font-family: 'Lora', serif;
    text-align: center;
    z-index: 999;
  }

  /* Close button */
  #uploadModal .close-button {
    position: absolute;
    top: 12px;
    right: 16px;
    font-size: 1.5rem;
    color: #666;
    cursor: pointer;
  }
</style>
